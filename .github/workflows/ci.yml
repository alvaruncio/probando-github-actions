name: CI

on:
  push:
  pull_request:

jobs:
  test-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify GitHub Actions execution
        run: echo "GitHub Actions is working"
